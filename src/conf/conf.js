const conf = {
    appwriteUrl: String(import.meta.env.VITE_APP_APPWRITE_URL),

    appwriteUrl: String(import.meta.env.VITE_APP_PROJECT_ID),
    appwriteUrl: String(import.meta.env.VITE_APP_COLLECTION_ID),
    appwriteUrl: String(import.meta.env.VITE_APP_DATABASE_ID),
    appwriteUrl: String(import.meta.env.VITE_APP_BUCKET_ID),}
    export default conf